@*@model Post;


    <h1></h1>

    <img class="card-img-top" src="@Url.Action("GetImage","Home", new {id=Model.Id})" alt="Uploaded Image" data-holder-rendered="true" width="100%" height="225" style="height: 225px; width: 100%; display: block;">
    <p class="card-text">@Model.Title</p>
    <p class="card-text">@Model.Text</p>

    <h1>the detail page of the post with id </h1>*@

@model Post

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@Model.Title</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

    <div class="container py-5">
        <div class="row justify-content-center">
            <div class="col-lg-8">
                <div class="card shadow-sm rounded-4 border-0">
                    <img class="card-img-top" src="@Url.Action("GetImage", "Home", new {id = Model.Id})" alt="Uploaded Image" data-holder-rendered="true" style="height: 225px; width: 100%; display: block;">
                    <div class="card-body">
                        <h1 class="card-title text-center">@Model.Title</h1>
                        <p class="card-text">@Model.Text</p>

                        <hr />
                        <h5 class="mt-4">Post Details</h5>
                        <p class="text-muted">Post ID: @Model.Id</p>
                        @*<p class="text-muted">Created on: @Model.CreatedAt.ToString("MMMM dd, yyyy")</p>*@

                        <div class="text-center mt-4">
                            @*<a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-warning btn-lg">Edit Post</a>*@
                            @*<a asp-action="Delete" asp-route-id="@Model.Id" class="btn btn-danger btn-lg ms-2">Delete Post</a>*@
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>